# ═══════════════════════════════════════════════════════════════
# Cadencia — Caddy Reverse Proxy Configuration
# context.md §7: TLS 1.3 minimum on all external connections
# context.md §14: security headers, rate limiting
#
# Local dev:  localhost:80 → api:8000 (auto-TLS disabled)
# Production: api.cadencia.in → ALB → ECS (Let's Encrypt TLS)
# ═══════════════════════════════════════════════════════════════

# ── Local development ──────────────────────────────────────────
:80 {
    # Security headers (defense-in-depth — also set in FastAPI)
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        X-XSS-Protection "1; mode=block"
        Referrer-Policy "strict-origin-when-cross-origin"
        Permissions-Policy "geolocation=(), microphone=(), camera=()"
        Content-Security-Policy "default-src 'none'"
        -Server
    }

    # SSE endpoints: disable buffering for real-time streaming
    @sse {
        path /v1/sessions/*/stream
    }
    header @sse {
        Cache-Control "no-cache"
        X-Accel-Buffering "no"
        Connection "keep-alive"
    }

    # Health check passthrough (no auth)
    handle /health {
        reverse_proxy api:8000
    }

    # Block internal-only paths from external access
    respond /metrics 403
    respond /docs 404
    respond /redoc 404
    respond /openapi.json 404

    # All API traffic → FastAPI
    reverse_proxy api:8000 {
        transport http {
            dial_timeout 5s
            response_header_timeout 120s
        }
        header_up X-Real-IP {remote_host}
        header_up X-Forwarded-Proto {scheme}
    }

    # Access logging (JSON format)
    log {
        output stdout
        format json
        level INFO
    }
}

# ── Production (uncomment and replace for deployment) ──────────
# api.cadencia.in {
#     tls {
#         protocols tls1.3
#     }
#
#     header {
#         Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
#         X-Content-Type-Options "nosniff"
#         X-Frame-Options "DENY"
#         X-XSS-Protection "1; mode=block"
#         Referrer-Policy "strict-origin-when-cross-origin"
#         Permissions-Policy "geolocation=(), microphone=(), camera=()"
#         Content-Security-Policy "default-src 'none'"
#         -Server
#     }
#
#     @sse {
#         path /v1/sessions/*/stream
#     }
#     header @sse {
#         Cache-Control "no-cache"
#         X-Accel-Buffering "no"
#     }
#
#     handle /health {
#         reverse_proxy cadencia-alb.ap-south-1.elb.amazonaws.com
#     }
#
#     respond /metrics 403
#     respond /docs 404
#     respond /redoc 404
#
#     reverse_proxy cadencia-alb.ap-south-1.elb.amazonaws.com {
#         transport http {
#             dial_timeout 5s
#             response_header_timeout 120s
#             keep_alive 30s
#         }
#         header_up X-Real-IP {remote_host}
#         header_up X-Forwarded-Proto {scheme}
#     }
#
#     log {
#         output stdout
#         format json
#         level INFO
#     }
# }
